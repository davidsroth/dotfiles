{
  "_comment_1": "Copy this file to ~/.pi/agent/slack-mcp.json (NOT stowed; per-machine secrets).",
  "_comment_2": "Use one of the auth methods below. Priority: XOXP > XOXB > XOXC+XOXD.",

  "command": "npx",
  "args": ["-y", "slack-mcp-server@latest", "--transport", "stdio"],

  "env": {
    "SLACK_MCP_XOXP_TOKEN": "xoxp-..."
  },

  "_comment_xoxb": "Bot token alternative:",
  "_env_xoxb_example": { "SLACK_MCP_XOXB_TOKEN": "xoxb-..." },

  "_comment_xoxc": "Stealth (browser session) alternative; see https://github.com/korotovsky/slack-mcp-server/blob/master/docs/01-authentication-setup.md",
  "_env_xoxc_example": {
    "SLACK_MCP_XOXC_TOKEN": "xoxc-...",
    "SLACK_MCP_XOXD_TOKEN": "xoxd-..."
  },

  "autoConnect": true,
  "toolPrefix": "slack_",
  "startupTimeoutMs": 30000,

  "_comment_timeout": "Per-tool-call timeout (ms, default 60000). Raise globally and/or per upstream tool for slow calls like conversations_unreads over many channels.",
  "requestTimeoutMs": 60000,
  "requestTimeoutMsByTool": { "conversations_unreads": 180000 },

  "_comment_postprocess": "CSV output cleanup (default on). Drops wide rarely-used columns, truncates long Text (maxTextLength, 0=off), and resolves <@U...> mentions to names. Dropping Cursor preserves pagination via a 'next_cursor:' footer. Set \"postProcess\": false for fully raw output, or omit a column from dropColumns to keep it (e.g. Permalink).",
  "postProcess": {
    "dropColumns": ["Permalink", "AttachmentIDs", "HasMedia", "BotName", "Cursor"],
    "maxTextLength": 800,
    "resolveMentions": true
  }
}
