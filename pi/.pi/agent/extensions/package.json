{
  "name": "pi-extensions-dev",
  "private": true,
  "version": "0.0.0",
  "description": "Dev-only manifest: vitest for the suites in _tests/. pi loads extensions via jiti and never reads this file.",
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "vitest": "^4"
  }
}
