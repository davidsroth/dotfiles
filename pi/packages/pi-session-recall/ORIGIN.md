# Origin and attribution

This is a local implementation informed by the two-stage `session_search` / `session_query` interaction in:

- `@ogulcancelik/pi-session-recall`
- https://github.com/ogulcancelik/pi-extensions/tree/main/packages/pi-session-recall

The implementation here was rewritten for this repository's stricter policy: structural visible-text extraction, branch-aware evidence, secure path validation, bounded asynchronous discovery, secret masking, source hashes, evidence citations, and explicit current-state caveats. It does not vendor the upstream source.
