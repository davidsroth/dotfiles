# Origin

Original package authored in-tree as part of the dotfiles repo.

Not vendored from anywhere. If this is upstreamed to its own repository,
replace this file with `VENDORED_FROM.md` per `packages/README.md`.

## Provenance

- The broker bridge targets stock `pi-intercom@0.9.2` and the v0.9.2-based
  `@davidroth/pi-intercom@0.10.x`, including portable `aside-v1`.
- The in-repo core reference is [`../pi-intercom`](../pi-intercom).
