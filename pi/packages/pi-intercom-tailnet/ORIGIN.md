# Origin

Original package authored in-tree as part of the dotfiles repo.

Not vendored from anywhere. If/when this is upstreamed to its own repo,
replace this file with `VENDORED_FROM.md` per the policy in
`packages/README.md`.

## Provenance

- Designed against David's `@davidroth/pi-intercom` fork (directory:
  `packages/pi-intercom`), originally vendored from `pi-intercom@0.6.0`.
- Scope doc: `tmp/pi-intercom-tailnet-scope.md` (kept until first
  external release).
