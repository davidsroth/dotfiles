# ============================================================================
# Starship Prompt Configuration - Minimal & Fast
# ============================================================================
# Only modules that appear in `format` are evaluated. Keep this file trimmed
# to what the prompt actually renders; starship ships sensible defaults for
# everything else.

# Global settings
command_timeout = 800        # ms (lower is better: large values let slow git_status run longer before cancellation — see starship#6065)
scan_timeout = 30            # ms for scanning files
add_newline = true
palette = "catppuccin_mocha"

# Format: user@host in dir (BRANCH) status
# Newline + character on second line.
format = """
$username$hostname in $directory$git_branch$git_status$line_break$character"""

# ============================================================================
# Module Configurations (only modules referenced in `format`)
# ============================================================================

[character]
success_symbol = "[❯](bold fg:green)"
error_symbol = "[❯](bold fg:red)"
vimcmd_symbol = "[❮](bold fg:green)"

[directory]
truncation_length = 3
truncate_to_repo = true
style = "bold fg:blue"
read_only = " 󰌾"
read_only_style = "fg:red"
home_symbol = "~"

[git_branch]
symbol = " "
style = "fg:mauve"
truncation_length = 20

[git_status]
style = "fg:red"
# Perf: skip the expensive untracked-file scan and submodule status.
# Empty `untracked` makes starship pass --untracked-files=no; ignore_submodules
# passes --ignore-submodules. Together these are the big win in large repos
# (starship#2625: 156ms -> 1.6ms). Tradeoff: no `?` untracked indicator.
untracked = ""
ignore_submodules = true
conflicted = "="
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
stashed = "\\$"
modified = "!"
staged = "+"
renamed = "»"
deleted = "✘"

# git_state is rendered implicitly during rebase/merge/cherry-pick even
# when not in `format`; style it so those banners match the palette.
[git_state]
style = "fg:peach"

[hostname]
# Show short host (before first dot). Only on SSH.
trim_at = "."
ssh_only = true
format = "[@$hostname]($style)"
style = "bold fg:mauve"
disabled = false
ssh_symbol = " "

[username]
format = "[$user]($style)"
style_root = "bold fg:red"
style_user = "bold fg:yellow"
show_always = true
disabled = false

# ============================================================================
# Palette - Catppuccin Mocha
# ============================================================================

[palettes.catppuccin_mocha]
text = "#cdd6f4"
subtext1 = "#bac2de"
subtext0 = "#a6adc8"
overlay2 = "#9399b2"
overlay1 = "#7f849c"
overlay0 = "#6c7086"
surface2 = "#585b70"
surface1 = "#45475a"
surface0 = "#313244"
base = "#1e1e2e"
mantle = "#181825"
crust = "#11111b"
blue = "#89b4fa"
lavender = "#b4befe"
sapphire = "#74c7ec"
sky = "#89dceb"
teal = "#94e2d5"
green = "#a6e3a1"
yellow = "#f9e2af"
peach = "#fab387"
maroon = "#eba0ac"
red = "#f38ba8"
mauve = "#cba6f7"
pink = "#f5c2e7"
flamingo = "#f2cdcd"
rosewater = "#f5e0dc"
