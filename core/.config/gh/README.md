# GitHub CLI (`gh`)

The official GitHub command-line tool.

- **Docs**: <https://cli.github.com/manual/>
- **Repo**: <https://github.com/cli/cli>

## Local customizations

`config.yml` uses `git_protocol: https`, keeps prompting enabled, and defines a single alias (`co: pr checkout`). Colors and accessibility options are left at defaults.
